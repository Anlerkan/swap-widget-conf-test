export enum NetworkToggleValue {
  MainNet = "mainnet",
  TestNet = "testnet"
}

export enum ConfigurationView {
  Swap = "swap",
  Connection = "connection",
  Styling = "styling",
  Export = "export"
}
